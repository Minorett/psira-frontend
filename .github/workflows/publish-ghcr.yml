name: Build & Publish Docker image to GHCR

on:
  push:
    branches:
      - develop
      - main
    tags:
      - "v*.*.*"

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }} # org/repo

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
        
      - name: Compute tags
        id: meta
        shell: bash
        run: |
          REPO=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')

          TAGS=""
          if [[ "${GITHUB_REF_TYPE}" == "branch" ]]; then
            if [[ "${GITHUB_REF_NAME}" == "develop" ]]; then
              TAGS="${{ env.REGISTRY }}/$REPO:staging"
            elif [[ "${GITHUB_REF_NAME}" == "main" ]]; then
              TAGS="${{ env.REGISTRY }}/$REPO:main"
            fi
          elif [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            TAGS="${{ env.REGISTRY }}/$REPO:${GITHUB_REF_NAME},${{ env.REGISTRY }}/$REPO:latest"
          fi

          echo "tags=$TAGS" >> "$GITHUB_OUTPUT"


      - name: Compute build args
        id: buildargs
        shell: bash
        run: |
          # Default to production unless we're explicitly on develop
          NG_CONF="production"

          if [[ "${GITHUB_REF_TYPE}" == "branch" ]]; then
            if [[ "${GITHUB_REF_NAME}" == "develop" ]]; then
              NG_CONF="staging"
            elif [[ "${GITHUB_REF_NAME}" == "main" ]]; then
              NG_CONF="production"
            fi
          elif [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            NG_CONF="production"
          fi

          echo "ng_conf=$NG_CONF" >> "$GITHUB_OUTPUT"


      - name: Build & Push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          build-args: |
            NG_CONF=${{ steps.buildargs.outputs.ng_conf }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
